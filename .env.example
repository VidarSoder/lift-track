# Server only. Never prefix these with NEXT_PUBLIC_.
ACCESS_PASSPHRASE=
ATHLETE_SALT=
SESSION_SECRET=
FIREBASE_SERVICE_ACCOUNT=
# Local fallback path (do not commit the file):
# GOOGLE_APPLICATION_CREDENTIALS=.secrets/firebase-admin.json
